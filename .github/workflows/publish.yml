name: Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: Verify tag matches Cargo version
        run: |
          # Strip leading "v" from the tag. Extract whatever sits between the
          # quotes in `version = "..."` — this captures full SemVer including
          # pre-release suffixes like 0.3.0-rc.1 (the old [0-9]+\.[0-9]+\.[0-9]+
          # regex silently truncated the suffix and broke rc releases).
          TAG="${GITHUB_REF#refs/tags/v}"
          VER="$(grep -E '^version\s*=\s*"' Cargo.toml | head -n1 | sed -E 's/^version\s*=\s*"([^"]+)".*/\1/')"
          if [ "$TAG" != "$VER" ]; then
            echo "Version mismatch: tag v$TAG != Cargo.toml $VER"
            exit 1
          fi
      - name: Verify python crate Cargo.toml version
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          PYVER="$(grep -E '^version\s*=\s*"' python/Cargo.toml | head -n1 | sed -E 's/^version\s*=\s*"([^"]+)".*/\1/')"
          if [ "$TAG" != "$PYVER" ]; then
            echo "Version mismatch: tag v$TAG != python/Cargo.toml $PYVER"
            exit 1
          fi

  linux:
    needs: [verify-version]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - runner: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: 'true'
          manylinux: auto
          working-directory: python
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: python/dist

  windows:
    needs: [verify-version]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
          architecture: ${{ matrix.platform.target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: 'true'
          working-directory: python
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: python/dist

  macos:
    needs: [verify-version]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-14
            target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: 'true'
          working-directory: python
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: python/dist

  sdist:
    needs: [verify-version]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
          working-directory: python
      - name: Upload sdist
        uses: actions/upload-artifact@v7
        with:
          name: wheels-sdist
          path: python/dist

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [linux, windows, macos, sdist]
    environment: pypi
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist
      - name: Extract this release's section from CHANGELOG.md
        # The curated CHANGELOG.md is the single source of truth. Pull only the
        # section for the tag being released (## [<version>] … up to the next
        # ## [) for the GitHub Release body; fall back to the whole file if the
        # section isn't found.
        run: |
          VER="${GITHUB_REF_NAME#v}"
          awk -v ver="$VER" '
            $0 ~ ("^## \\[" ver "\\]") {f=1; print; next}
            f && /^## \[/ {exit}
            f {print}
          ' CHANGELOG.md > RELEASE_NOTES.md
          if [ ! -s RELEASE_NOTES.md ]; then
            echo "No section for $VER in CHANGELOG.md; using full file."
            cp CHANGELOG.md RELEASE_NOTES.md
          fi
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          skip-existing: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        if: success() || failure()
        with:
          body_path: RELEASE_NOTES.md
          files: dist/*
          # SemVer pre-releases all contain a `-` (e.g. 0.3.0-rc.1, 0.3.0-beta.2).
          # The action's auto-detection didn't catch v0.3.0-rc.1 on first run,
          # so set the flag explicitly from the tag name.
          prerelease: ${{ contains(github.ref_name, '-') }}

  crates-io:
    runs-on: ubuntu-latest
    needs: [release]
    environment: release
    permissions:
      id-token: write
      contents: read
    steps:
    - uses: actions/checkout@v7
    - uses: rust-lang/crates-io-auth-action@v1
      id: auth
    - run: cargo publish --locked || echo "Already published"
      env:
        CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}