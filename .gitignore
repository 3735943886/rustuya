# Rust build output
target/
Cargo.lock
*/target/
*/Cargo.lock

# Local Python venv for the oracle tooling (tuyamock / tinytuya)
.venv/

# Editor / tooling & OS — local, machine-specific
.claude/
.DS_Store
